# Agree on UI element semantics before implementation

Before implementing display elements (badges, labels, icons), agree with the PO
on what they semantically represent. Implementation of display conditions follows
from the semantic definition.

## Example

The recurrence badge means "configured as recurring", not "will generate next instance".
