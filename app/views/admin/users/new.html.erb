<% content_for :title, "新しいユーザー作成" %>

<div class="admin-user-new">
  <div class="admin-header-section">
    <h1>新しいユーザー作成</h1>
    
    <div class="admin-actions">
      <%= link_to "一覧に戻る", admin_users_path, class: "btn btn-outline" %>
    </div>
  </div>

  <%= form_with model: [:admin, @user], local: true do |form| %>
    <div class="form-container">
      <% if @user.errors.any? %>
        <div class="form-errors">
          <h3>エラーが発生しました</h3>
          <ul>
            <% @user.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-section">
        <h2>基本情報</h2>
        
        <div class="form-group">
          <%= form.label :email, "メールアドレス", class: "form-label" %>
          <%= form.email_field :email, class: "form-input", placeholder: "user@example.com", required: true %>
        </div>

        <div class="form-group">
          <%= form.label :name, "名前", class: "form-label" %>
          <%= form.text_field :name, class: "form-input", placeholder: "ユーザー名（任意）" %>
        </div>

        <div class="form-group">
          <%= form.label :password, "パスワード", class: "form-label" %>
          <%= form.password_field :password, class: "form-input", placeholder: "8文字以上", required: true %>
          <small class="form-help">8文字以上で入力してください</small>
        </div>

        <div class="form-group">
          <%= form.label :password_confirmation, "パスワード確認", class: "form-label" %>
          <%= form.password_field :password_confirmation, class: "form-input", placeholder: "パスワードを再入力", required: true %>
        </div>
      </div>

      <div class="form-section">
        <h2>設定</h2>
        
        <div class="form-group">
          <%= form.label :time_zone, "タイムゾーン", class: "form-label" %>
          <%= form.select :time_zone, options_for_select([
            ["Asia/Tokyo", "Asia/Tokyo"],
            ["UTC", "UTC"],
            ["America/New_York", "America/New_York"],
            ["Europe/London", "Europe/London"]
          ], "Asia/Tokyo"), { prompt: "選択してください" }, { class: "form-input" } %>
        </div>

        <div class="form-group">
          <%= form.label :locale, "言語", class: "form-label" %>
          <%= form.select :locale, options_for_select([
            ["日本語", "ja"],
            ["English", "en"]
          ], "ja"), { prompt: "選択してください" }, { class: "form-input" } %>
        </div>
      </div>

      <div class="form-actions">
        <%= form.submit "ユーザーを作成", class: "btn btn-primary" %>
        <%= link_to "キャンセル", admin_users_path, class: "btn btn-outline" %>
      </div>
    </div>
  <% end %>

  <div class="form-help-section">
    <h3>ユーザー作成について</h3>
    <ul>
      <li>作成後にロールを割り当てることができます</li>
      <li>ユーザーは最初の自動でinboxプロジェクトが作成されます</li>
      <li>TOTPの設定はユーザー自身が行います</li>
    </ul>
  </div>
</div>