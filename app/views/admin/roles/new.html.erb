<% content_for :title, "新しいロール作成" %>

<div class="admin-role-new">
  <div class="admin-header-section">
    <h1>新しいロール作成</h1>
    
    <div class="admin-actions">
      <%= link_to "一覧に戻る", admin_roles_path, class: "btn btn-outline" %>
    </div>
  </div>

  <%= form_with model: [:admin, @role], local: true do |form| %>
    <div class="form-container">
      <% if @role.errors.any? %>
        <div class="form-errors">
          <h3>エラーが発生しました</h3>
          <ul>
            <% @role.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-section">
        <h2>基本情報</h2>
        
        <div class="form-group">
          <%= form.label :name, "ロール名", class: "form-label" %>
          <%= form.text_field :name, class: "form-input", placeholder: "例: content_manager" %>
          <small class="form-help">英数字とアンダースコアのみ使用可能です</small>
        </div>

        <div class="form-group">
          <%= form.label :description, "説明", class: "form-label" %>
          <%= form.text_area :description, class: "form-input", rows: 3, placeholder: "このロールの説明を入力してください" %>
        </div>
      </div>

      <div class="form-actions">
        <%= form.submit "ロールを作成", class: "btn btn-primary" %>
        <%= link_to "キャンセル", admin_roles_path, class: "btn btn-outline" %>
      </div>
    </div>
  <% end %>

  <div class="form-help-section">
    <h3>ロール作成について</h3>
    <ul>
      <li>作成後に権限を割り当てることができます</li>
      <li>ロール名は後から変更可能です</li>
      <li>システムロールとは異なり、カスタムロールは削除可能です</li>
    </ul>
  </div>
</div>